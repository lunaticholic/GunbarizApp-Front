import React from 'react';
import Icon from 'react-native-vector-icons/AntDesign';

const BackButton = () => {
    return (
        <Icon name="left" size={30} style={{paddingLeft: 10}} />
    )
};

export default BackButton;